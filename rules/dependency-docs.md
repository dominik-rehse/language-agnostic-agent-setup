# Dependency / library / API documentation

Use the Context7 MCP for dependency/library/API documentation without being asked. Specify the library version upfront. If the library ID isn't known, resolve it via `resolve-library-id` first.
